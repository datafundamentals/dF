# Ticket: New .yaml vscode extension

If you were to look at extensions/df-markdown-tools you would notice that there is a vscode extension which currently does a token count on markdown files. This is a foundation only, and it will be expanded to offer more important functionality.

Now I need to create the exact same thing in a completely new extensions/df-yaml-tools, which will again start with the same functionality but then later be used to expand into new functionality sets. So this new df-yaml-tools extension would be identical except it is for .yaml files, and it would also need a different icon for opening it, looking a bit more tree like. Instead of the hover message of "Open DF Tools" it would say "Open DF yaml Tools"

Please consider a step by step approach if it suits your workstyle. It helps me be consulted during the process.