# Standardization Follow-ups

Track future documentation or standards work here so `guides/` stays limited to in-scope tickets.

- Reintroduce Firebase standards after `apps/df-firebase-teaching-app0` exists; update `guides/STANDARDS_STYLES.md` and `guides/WC_SHARED_DEFAULTS.md` with concrete guidance tied to the implementation.
- Design a shared reference for external 11ty deployments once at least one production bundle is integrated; decide whether the repo needs a canonical sample 11ty site.
