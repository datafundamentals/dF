# Activity Log Tests

Playwright tests live in `tests/integration`. They should cover:
- Authentication guard rendering
- Pushup form submission against the emulator
- History list pagination when added later

Tests are intentionally deferred for this first iteration so we can validate the UI/Firestore flow manually while the emulator story stabilizes.
