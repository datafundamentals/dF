# DF App Starter Template

this readme became a 1000 line doc in a previous iteration. 

This new iteration needs to be limited to:

1. Basic high level workflow outline
1. Basic splainer on how to access TMI that you might need beyond this.
1. TOC with links to relevant external docs as needed from the root guides/

Emulators: `pnpm --filter @df/df-app-starter-template emulators:start` uses the shared port map from `packages/firebase/firebase.json`; keep this app’s `firebase.json` aligned to avoid drift.
