# Chat App Tests

These Playwright specs will live in `tests/integration` once the chat flow stabilises.

Tests should eventually cover:
- Authentication guard rendering
- Message submission and optimistic UI states
- Realtime transcript updates and pagination (as features land)

Until then we are deferring automation and relying on manual validation of the widget harness.
