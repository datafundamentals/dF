rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development/teaching rules - EMULATOR ONLY
    // For production, implement proper authentication-based rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
