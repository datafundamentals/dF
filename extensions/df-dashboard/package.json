{
  "name": "df-dashboard",
  "displayName": "DF Dashboard",
  "description": "Dashboard for DF Monorepo",
  "version": "0.0.1",
  "publisher": "datafundamentals",
  "repository": {
    "type": "git",
    "url": "https://github.com/datafundamentals/dF"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "df.openDashboard",
        "title": "Open Dashboard",
        "category": "DF"
      }
    ]
  },
  "scripts": {
    "build": "pnpm run build:ui && pnpm run copy:ui && pnpm run package",
    "vscode:prepublish": "pnpm run build",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "tsup src/extension.ts --format cjs --external vscode",
    "lint": "eslint ./src",
    "build:ui": "pnpm --filter @df/df-dashboard-ui build",
    "copy:ui": "rm -rf media/ui && mkdir -p media/ui && cp -r ../df-dashboard-ui/dist/* media/ui/"
  },
  "devDependencies": {
    "@df/df-dashboard-ui": "workspace:*",
    "@df/types": "workspace:*",
    "@types/node": "20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.46.1",
    "@typescript-eslint/parser": "^8.46.1",
    "eslint": "^9.0.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@df/node-utils": "workspace:*"
  }
}
