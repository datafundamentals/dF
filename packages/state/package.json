{
  "name": "@df/state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "lint": "echo 'No lint targets yet'",
    "prebuild": "node scripts/generate-auth-config.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@df/firebase": "workspace:*",
    "@df/types": "workspace:*",
    "@lit-labs/signals": "^0.1.0",
    "firebase": "^11.7.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
